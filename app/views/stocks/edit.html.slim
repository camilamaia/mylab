.page-header
  h3 = t('stock.edit.title')

= render 'form'

.page-header
  h3 = t('stock.edit.destroy')

p = t('stock.edit.destroy_alert')

= link_to "Excluir Estoque", stock_path(@stock), method: :delete, data: { confirm: t('stock.edit.destroy_confirmation', stock_name: @stock.name)}, class: "btn btn-danger"
