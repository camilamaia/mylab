<%= simple_form_for @material do |f| %>
  <%= f.input :name, label: 'Nome' %> <br>
  <%= f.input :formula, label: 'Fórmula' %> <br>
  <%= f.input :kind, label: false, :selected => 1 %> <br>
  <%= f.submit 'Salvar Material' %>
<% end %>
